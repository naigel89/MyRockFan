package com.example.myrockfan.ui.theme

import androidx.compose.ui.graphics.Color

// Negros y Grises (Fondo)
val RockBlack = Color(0xFF121212) // Fondo principal (no es negro puro, es más elegante)
val RockSurface = Color(0xFF1E1E1E) // Para tarjetas
val RockSurfaceVariant = Color(0xFF2C2C2C)

// Acentos
val ElectricPurple = Color(0xFFBB86FC) // Un morado clásico de neón
val HotPink = Color(0xFFEF3868) // Para detalles vibrantes

// Textos
val TextWhite = Color(0xFFEEEEEE) // Blanco hueso
val TextGrey = Color(0xFFB0B0B0) // Texto secundario